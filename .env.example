# Azure DevOps Personal Access Token (required)
# Create a PAT with "Code (Read)" scope at:
# https://dev.azure.com/{your-org}/_usersSettings/tokens
AZURE_DEVOPS_PAT=your_pat_token_here

# Optional: Default organization/project/repository
# These are used as fallbacks when not running from a git repository
ADO_ORGANIZATION=your-organization
ADO_PROJECT=your-project
ADO_REPOSITORY=your-repository

# Optional: MCP Server Configuration
MCP_TRANSPORT=stdio
MCP_LOG_LEVEL=INFO
